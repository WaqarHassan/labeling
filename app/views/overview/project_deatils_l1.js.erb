$("#overview_detail").html("<%= escape_javascript(render 'overview/project_details') %>");

$(document).ready(function() {
	 $('.collapse_table_data').removeClass('collapse_table');
	 $('#myPleaseWait_search_all').modal('hide');	
});

if($("#selection_msg_div").length){	
	$('#selection_msg_div').remove();
}
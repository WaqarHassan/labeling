
<div class="modal fade" id="info_modal_popup" role="dialog"></div>


<div class="modal fade" id="rework_modal_popup" role="dialog"></div>
<div class="modal fade" id="confirm_modal_popup" role="dialog"></div>
<div class="modal fade" id="add_l2_modal" role="dialog"> </div>
<div class="modal fade" id="reject_reason_modal" role="dialog"> </div>

<div class="modal fade" id="add_l1_modal" role="dialog"></div>
<div class="modal fade" id="update_workflow_modal" role="dialog"></div>


<div class="modal fade" id="project_status_popup" role="dialog">
  
</div>
<div class="modal fade" id="l2_update_popup" role="dialog">
  
</div>
<div class="modal fade" id="l3_update_popup" role="dialog">
  
</div>


<div class="modal fade" id="project_update_popup" role="dialog">
  <div class='modal-dialog'>
      <div class='modal-content'>
        <div class='modal-header'>
        
           <button type='button' class='close' data-dismiss='modal'>×</button>
              <h1 class='h1-heading-popup'>Project Update</h1>
         </div>
        <div class='modal-body' id="project_update_popup_body">
            
      </div>

      <div class="modal-footer">

        <button class="btn btn-danger" data-dismiss="modal" type="button">Close</button>
        <button class="btn save-print-update-edit-btn"  onclick="addProjectSubmit();" type="submit"   >Update</button>
    </div>
      </div>
  </div>
</div>   

<div class="modal fade" id="add_l3_modal" role="dialog"></div>  

<% if session[:open_confirm_modal] == 'open_confirm_modal'%>
    <script type="text/javascript">
      $(document).ready(function() {
        workflow_step = '<%= session[:workflow_step_id] %>';
        $.get( "/overview/open_confirm_modal/"+workflow_step, function( data ) {
          $( "#add_l2_modal" ).html( data );
          $("#add_l2_modal").modal('show');
        });
      });
    </script>
  <% end %>

<% if session[:open_reason_modal] == 'open_reason_modal'%>
    <script type="text/javascript">
      $(document).ready(function() {
        l2_id = '<%= session[:l2_id] %>';
        $.get( "/overview/reject_reason_modal/"+l2_id, function( data ) {
          $( "#reject_reason_modal" ).html( data );
          $("#reject_reason_modal").modal('show');
        });
      });
    </script>
  <% end %>

<ul>
  <li>
    <%= link_to 'Terms', terms_path %>
  </li>
  <li>
    <%= link_to 'Privacy', privacy_path %>
  </li>
</ul>


